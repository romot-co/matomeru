/**
 * Common constants used throughout the extension
 */

/**
 * Conversion factor for estimating tokens from byte count
 * Based on empirical observation of typical text to token ratios
 */
export const TOKENS_PER_BYTE = 3.6;